import { CustomCheckBox } from '@/components';
import { CheckboxProps } from '@mui/material/Checkbox';

export function TableCheckBox(props: CheckboxProps) {
  return <CustomCheckBox {...props} />;
}
